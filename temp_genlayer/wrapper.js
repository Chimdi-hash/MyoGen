import { createClient } from 'genlayer-js'; import { studionet } from 'genlayer-js/chains'; import { custom } from 'viem'; window.callGenLayer = async function(contract, method, args, accountAddress) { if(!accountAddress) throw new Error('Account address is missing'); const client = createClient({ chain: studionet, transport: custom(window.ethereum) }); const txHash = await client.writeContract({ address: contract, functionName: method, args: args, account: { address: accountAddress } }); const receipt = await client.waitForTransactionReceipt({ hash: txHash }); if (receipt.status === 'reverted') { console.error('GenLayer Reverted', receipt); throw new Error('Transaction reverted by GenLayer Simulator.'); } return txHash; }; window.readGenLayer = async function(contract, method, args) { const client = createClient({ chain: studionet, transport: window.ethereum ? custom(window.ethereum) : undefined }); return await client.readContract({ address: contract, functionName: method, args: args }); };